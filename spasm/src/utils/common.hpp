#pragma once

#include <string>
#include <filesystem>
#include <format>
#include <sputils.hpp>

namespace spasm
{
    using namespace spade;
    using std::string;
    namespace fs = std::filesystem;
}    // namespace spasm